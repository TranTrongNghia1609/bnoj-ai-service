/**
 * Re-exports from the shared prompt builder.
 * Kept for backward compatibility with any direct imports from this path.
 */
export {
  buildDraftPrompt,
  buildRefinerPrompt,
  buildGeminiPrompt,
} from '../shared/promptBuilder.js';
